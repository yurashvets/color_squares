import React from 'react';

const CountDownTimer = () => {
  return (
    <div style={{flex: 1}}>
      <p style={{color: "white", fontSize: '55px', textAlign: 'center'}}>Timer</p>
    </div>
  )
}

export default CountDownTimer;
